extends ../layouts/default

block intro
	.container
		h1= __("Contact Us")
		
block content
	.container
		if enquirySubmitted
			h3.green-text= __("Thanks for getting in touch.")
		else
			.row: .col-sm-8.col-md-6
				form(method='post')
					input(type='hidden', name='action', value='contact')
					.form-group(class=validationErrors.name ? 'has-error' : null)
						label= __("Name")
						input(type='text', name='name', value=formData['name']).form-control
					.form-group(class=validationErrors.email ? 'has-error' : null)
						label= __("Email")
						input(type='email', name='email', value=formData.email).form-control
					.form-group
						label= __("Phone")
						input(type='text', name='phone', value=formData.phone, placeholder=__("Optional")).form-control
					// .form-group(class=validationErrors.enquiryType ? 'has-error' : null)
					// 	label What are you contacting us about?
					// 	select(name='enquiryType').form-control
					// 		option(value='') (select one)
					// 		each type in enquiryTypes
					// 			option(value=type.value, selected=formData.enquiryType == type.value)= type.label
					.form-group(class=validationErrors.message ? 'has-error' : null)
						label= __("Message")
						textarea(name='message' rows=4).form-control.materialize-textarea= formData.message
					.form-actions
						button(type='submit').btn.blue= __("Send")
